# D Examples for ZeroMQ
Examples for using the [ZeroMQ](https://zeromq.org/) messaging library.

## Running
In one terminal, start the server:

```bash
$ dub run :server
```

and in another terminal, start the client:

```bash
$ dub run :client
```
